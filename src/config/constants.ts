export const CONFIG = {
  SUPPORTED_FILE_TYPES: ['jpg', 'jpeg', 'png', 'webp'],
  IMAGE_DIMS: {
    minWidth: 100,
    maxWidth: 5000,
    minHeight: 100,
    maxHeight: 5000
  },
  MAX_FILE_SIZE: 5 * 1024 * 1024 // 5MB
}; 